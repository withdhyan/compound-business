# Dogfood log — CB improving itself through real use

Convention (the continuous meta-reflection loop): every CB skill run inside a real project appends an entry here — what the skill was asked to do, where its text helped, where it chafed (missing mode, ambiguous step, absent output contract), and what fix was applied. Fixes follow /new-role discipline: match the form to the failure, fix only observed seams, one skill at a time. This log is also the raw material for the pre-v1.0 compounding demo.

## Entries

*(appended per run, newest first)*

- 2026-07-09 · **/cb-init** (values project — first run of the *published* plugin, installed via marketplace) · Exercise: the repair/verify path on an already-seeded repo (CB's birthplace). HELPED: idempotence rule made the run safe — found/missing report surfaced exactly one gap (outbox/README). CHAFED (2): (1) skill assumes the host repo has no prior skills — no step for the migration case where local pre-generalization copies shadow the plugin's (fix applied host-side: retired 11 local skills; candidate skill fix: add a "local duplicates?" check to step 1); (2) template expects a "Standing rules" bullet in BUSINESS.md's Current state, but a repo seeded pre-template had rules scattered across doc references — chief's gate check would have had no single surface to read (fix applied host-side: consolidated Standing-rules bullet; candidate skill fix: step 1 should verify the bullet exists, not just the file). Also: first live proof of the marketplace install path (`/plugin marketplace add withdhyan/compound-business` → project-scope enable).

- 2026-07-04 · **OWNER-CORRECTED (the most important entry so far)** · The agent's decision heuristic ("reversible + internal = agent decides") let two levers through silently: a positioning recommendation that became a narrative default, and a 15-context product taxonomy (an ontology commitment). Owner's diagnosis: no meta-loop identifying critical levers and checkpoints. FIX APPLIED: DECISIONS.md typed register (LEVER/JUDGMENT/MECHANICAL, typed by blast radius + inheritance, never file-reversibility) + /chief now runs a lever audit every cycle (blocks work building on open levers; retro-flags levers taken silently). Meta-lesson for CB itself: an autonomy contract without a decision ontology degrades into local heuristics — the register IS the lightweight ontology.

- 2026-07-04 · **/brand** (values project, positioning brief) · CHAFED: skill is name/one-liner shaped; "prep a positioning session" had no mode — no forcing question, no direction-comparison format. Borrowed gstack design-consultation's memorable-thing question ad hoc. FIX APPLIED: positioning mode added to brand skill (memorable-thing question + hostile-headline test per direction + recommend-one rule). HELPED: hostile-headline ground truth immediately structured the comparison.
- 2026-07-04 · **/counsel** (values project, product consent flow) · CHAFED: procedure is review-shaped (SHIP/FIX on a feature); *drafting* a consent flow had no output contract, so the gate format was improvised. FIX APPLIED: draft-mode contract added to counsel skill (drafts end with a gate: numbered "what must hold before ship," each item FIX-conditioned). HELPED: the four-questions frame and learnings-precedents transferred to drafting unchanged; layered-consent-at-moment-of-relevance emerged directly from the "never bundled" precedent.
- 2026-07-04 · **meta** · Pattern across both runs: every role skill is shaped for its most common verb (review, name) and chafes on its second verb (draft, position). Rule candidate for /new-role: when writing a role, list its 2-3 verbs and give each an output contract — one-verb skills grow second verbs in production.
